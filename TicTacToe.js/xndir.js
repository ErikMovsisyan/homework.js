/*function foo(arr) {
    for (let i = 0; i < arr.length; ++i) {
        if (typeof arr[i] != 'string') {
            throw new Error("invalid value")
        }
    }
    let len = 0;
    let result = [];
    for (let i = 0; i < arr.length; ++i) {
        len = arr[i].length;
        result.push(len);
        len = 0;

    }
    return result;
}
let arr = ["hello","erik","manvel"];
console.log(foo(arr))*/
console.log(undefined + 1)